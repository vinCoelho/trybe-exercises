// declarando variáveis e constante.

let myName = 'Vinicius';

const birthCity = 'Belo Horizonte';

let birthYear = 1987;

// exibindo variáveis e constante.

console.log(myName);

console.log(birthCity);

console.log(birthYear);

// alterando valor atribuído à variável birthYer e exibindo-a com o console.log

birthYear = 2030;

console.log(birthYear);

// alterando (tentando) valor atribuído à constante birthCity

birthCity = 'Curitiba';

console.log(birthCity);

// Erro acontece porque não é possível alterar o valor atribuído a uma constante (const)